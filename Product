<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Product Slide Carousel</title>
<style>
  :root{
    --bg:#eaf7ef;
    --card:#ffffff;
    --accent:#bfe8c9;
    --muted:#7b8a84;
    --green:#2e8b57;
    --shadow: 0 6px 18px rgba(46,75,52,0.08);
    --radius:14px;
    font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  }

  body{
    margin:0;
    background: linear-gradient(90deg,#e8f8ef 0%, #f5fbf9 100%);
    color:#103127;
    padding:30px;
    -webkit-font-smoothing:antialiased;
    -moz-osx-font-smoothing:grayscale;
  }

  .container{
    max-width:1100px;
    margin:0 auto;
  }

  h1{
    font-size:20px;
    margin:0 0 18px 0;
    color:#184c3a;
  }

  /* Slider */
  .slider{
    position:relative;
    background:var(--bg);
    padding:22px;
    border-radius:18px;
    box-shadow: var(--shadow);
  }

  .viewport{
    overflow:hidden;
    border-radius:12px;
  }

  .track{
    display:flex;
    gap:18px;
    transition:transform 420ms cubic-bezier(.22,.9,.34,1);
    padding:16px;
  }

  .card{
    min-width:260px;
    max-width:320px;
    flex:0 0 300px;
    background:var(--card);
    border-radius:12px;
    padding:16px;
    box-shadow: var(--shadow);
    display:flex;
    flex-direction:column;
    gap:12px;
  }

  .media{
    height:160px;
    border-radius:10px;
    background:
      linear-gradient(180deg, rgba(46,139,87,0.06), rgba(46,139,87,0.02)),
      radial-gradient(circle at 10% 10%, rgba(46,139,87,0.06), transparent 15%),
      #f8fffb;
    display:flex;
    align-items:center;
    justify-content:center;
    position:relative;
    overflow:hidden;
  }

  .media img{
    width:100%;
    height:100%;
    object-fit:cover;
    display:block;
    border-radius:10px;
  }

  .badge{
    position:absolute;
    left:10px;
    top:10px;
    background:var(--accent);
    color:var(--green);
    font-weight:600;
    padding:6px 8px;
    border-radius:9px;
    font-size:13px;
  }

  .title-row{
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:8px;
  }

  .title{
    font-size:16px;
    font-weight:700;
    margin:0;
  }

  .price{
    font-size:18px;
    color:var(--green);
    font-weight:800;
  }

  .desc{
    color:var(--muted);
    font-size:13px;
    line-height:1.4;
    margin:0;
  }

  .meta{
    display:flex;
    gap:10px;
    align-items:center;
    margin-top:auto;
  }

  .meta .rating{
    display:flex;
    gap:6px;
    align-items:center;
    font-size:13px;
    color:#2f6b4f;
  }

  .meta button{
    margin-left:auto;
    background:linear-gradient(180deg,#2f8f5a,#2b7e4f);
    color:white;
    border:none;
    padding:8px 12px;
    border-radius:10px;
    cursor:pointer;
    font-weight:700;
  }

  /* Controls */
  .controls{
    position:absolute;
    top:50%;
    transform:translateY(-50%);
    left:12px;
    right:12px;
    pointer-events:none;
  }
  .controls .btn{
    pointer-events:auto;
    background:var(--card);
    border-radius:10px;
    border:none;
    padding:8px;
    box-shadow:var(--shadow);
    cursor:pointer;
    display:inline-flex;
    align-items:center;
    justify-content:center;
  }
  .controls .prev{ position:absolute; left:4px;}
  .controls .next{ position:absolute; right:4px;}

  /* Dots */
  .dots{
    display:flex;
    gap:8px;
    justify-content:center;
    margin-top:16px;
  }
  .dot{
    width:10px;height:10px;border-radius:999px;background:#dfeee0;cursor:pointer;
  }
  .dot.active{ background:var(--green); transform:scale(1.15); }

  /* Responsive */
  @media (max-width:720px){
    .track{ gap:12px; padding:12px;}
    .card{flex:0 0 80%; min-width:220px;}
    .media{height:140px;}
  }
</style>
</head>
<body>
  <div class="container">
    <h1>Featured Products</h1>

    <div class="slider" aria-roledescription="carousel" aria-label="Featured products">
      <div class="viewport" id="viewport">
        <div class="track" id="track">

          <!-- Slide 1 -->
          <article class="card" role="group" aria-label="Product 1">
            <div class="media">
              <span class="badge">New</span>
              <!-- Placeholder SVG product image (replace with <img src="..."> ) -->
              <svg width="240
